#   3. Crie um algoritmo que calcule a potência de um número base elevado a um expoente.

base = int(input("Digite o numero base: "))
exp = float(input("Digite o numero exponencial: "))
print(round(pow(base, exp), 2))